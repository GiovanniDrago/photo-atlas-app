New update check: Settings -> About shows the installed version and a "Check for updates" button,
and the app also checks once a day in the background. When a newer release exists it offers to
download it (arm64 APK on Android, Linux bundle on desktop) and opens the browser.
