org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Long uploads run in a dataSync foreground service: the workmanager plugin
# needs this flag to declare FOREGROUND_SERVICE_DATA_SYNC and the matching
# service type (required on Android 14+ / targetSdk 34+).
workmanager.enableDataSyncForegroundService=true
